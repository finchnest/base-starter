try
{
    SarifLog sarif = SarifLog.Load(@"..\databases\NewBaseline.sarif");
    Console.WriteLine("value is " + sarif.Runs[0].Results.Count);

}
catch (Exception e)
{
    Console.WriteLine(e.Message);
}